// Dart
import 'package:flutter/material.dart';

class TitulacionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Métodos de Titulación')),
      body: Center(child: Text('Información sobre métodos de titulación')),
    );
  }
}