import 'package:flutter/material.dart';

class PracticasProfesionalesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('practicas profesionales')),
      body: Center(child: Text('Gestión de practicas profesionales')),
    );
  }
}